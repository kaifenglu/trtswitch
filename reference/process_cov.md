# helper: process a base covariate specification and ensure design columns exist in df

helper: process a base covariate specification and ensure design columns
exist in df

## Usage

``` r
process_cov(base_cov, df)
```
